#ifndef _FACTOR_H
#define _FACTOR_H

#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

/* size_t getline(char == lineptr, size_t *n FILE *stream); */
int factorize(char *buffer);

#endif /* _FACTOR_H */
	

